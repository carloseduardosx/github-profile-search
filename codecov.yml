codecov:
  token: 78e23405-13ba-4e8a-bfd2-2e6d74c9d158
  branch: master

coverage:
  precision: 2
  round: up
  range: "35...100"

  status:
    project:
      default:
        target: auto
        threshold: null
        base: auto
        branches: null
        set_pending: yes
        if_no_uploads: error
        if_not_found: success
        if_ci_failed: error
        flags: null
        paths: null

    patch:
      default:
        target: auto
        base: auto
        threshold: null
        branches: null
        if_no_uploads: error
        if_not_found: success
        if_ci_failed: error
        only_pulls: false
        flags: null
        paths: null

    changes:
      default:
        target: auto
        base: auto
        branches: null
        set_pending: yes
        if_no_uploads: error
        if_not_found: success
        if_ci_failed: error
        flags: null
        paths: null

comment:
  layout: "header, diff, changes, sunburst, suggestions, tree, uncovered"
  require_changes: false
  behavior: default
